steps for running the html server webpage thingy:

1. plug in and upload the arduino stuff to both boards
2. make sure arduino's serial monitor is closed (only 1 program can read it at a time and the server's supposed to do that)
3. run server.py in terminal
4. paste "http://localhost:5000" in a browser
